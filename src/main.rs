use std::collections::HashMap;

mod vectors;
mod strings;
mod hash_maps;
fn main() {
    // vectors::run();
    // vectors::iterate_values();
    // vectors::using_enum_to_store_multiple_types();
    // strings::run();
    hash_maps::run();
}
