use std::collections::HashMap;

mod hash_maps;
mod strings;
mod vectors;
fn main() {
    // vectors::run();
    // vectors::iterate_values();
    // vectors::using_enum_to_store_multiple_types();
    // strings::run();
    // hash_maps::run();
    hash_maps::updating_hash_map();
}
